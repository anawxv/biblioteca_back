# Copie para .env antes de subir o frontend com ngrok:
#   copy .env.ngrok.example .env
#
# Modo A — 1 tunel ngrok (recomendado plano free):
VITE_API_URL=/api
#
# Modo B — 2 tuneis (frontend + backend separados):
# VITE_API_URL=https://SEU-TUNEL-BACKEND.ngrok-free.app/api
